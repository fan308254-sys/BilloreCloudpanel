package docker

// Docker integration will be implemented in the next BilloreCloud agent phase.
// Keep Docker operations behind this package so the HTTP/API layer never executes
// arbitrary host commands directly.
